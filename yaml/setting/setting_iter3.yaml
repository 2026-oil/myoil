runtime:
  random_seed: 42
  opt_n_trial: 500
training:
  input_size: 128
  batch_size: 16
  valid_batch_size: 64
  windows_batch_size: 1024
  inference_windows_batch_size: 1024
  max_steps: 5000
  val_size: 16
  scaler_type: robust
  val_check_steps: 1
  min_steps_before_early_stop: 1000
  model_step_size: 1
  early_stop_patience_steps: 80
  loss: mse
  optimizer:
    name: adamw
    kwargs: {}
  lr_scheduler:
    name: ReduceLROnPlateau
    max_lr: 0.001
    mode: min
    factor: 0.5
    patience: 8
    threshold: 0.0001
    threshold_mode: rel
    cooldown: 3
    min_lr: 0.0
    eps: 0.00000001
cv:
  gap: 0
  horizon: 8
  step_size: 4
  n_windows: 6
  max_train_size: null
  overlap_eval_policy: by_cutoff_mean
scheduler:
  gpu_ids: [0, 1]
  max_concurrent_jobs: 2
  worker_devices: 1
