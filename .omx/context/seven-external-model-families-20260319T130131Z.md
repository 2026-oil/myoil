# Deep Interview Context Snapshot

- Task statement: Add seven external baseline model families to NeuralForecast using the representative upstream GitHub repositories named by the user.
- Desired outcome: An execution-ready specification for integrating the requested model families into this brownfield repository.
- Stated solution: Add Nonstationary Transformer, CMamba, Mamba, S-Mamba, xLSTM-Mixer, DUET, and DeepEDM.
- Probable intent hypothesis: Expand NeuralForecast baseline/model coverage with official public implementations while keeping the integration maintainable and verifiable.
- Known facts/evidence:
  - Repository is brownfield NeuralForecast package under `neuralforecast/`.
  - Current branch is `main`.
  - Target model files do not yet exist under `neuralforecast/models/`.
  - No existing `.omx/plans/` planning artifacts are present in this workspace.
  - Workspace is dirty in unrelated files; interview only, no implementation yet.
  - Prior memory suggests a previous seven-family planning/execution lane existed in another worktree, with non-goals around docs/phase1/new dependencies/paper-score reproduction.
- Constraints:
  - Need explicit non-goals and decision boundaries before execution handoff.
  - Must avoid asking user for repo facts that can be inspected directly.
  - Must not implement directly in deep-interview mode.
- Unknowns/open questions:
  - Whether scope is all seven families now or a prioritized subset.
  - Whether prior non-goals still apply in this workspace.
  - Whether Auto wrappers/export surface should be included now.
  - Acceptable dependency policy and fallback behavior for optional upstream packages.
  - Required verification depth (unit only, smoke training, full regression, etc.).
- Decision-boundary unknowns:
  - What OMX may decide alone about implementation order, CPU-only smoke defaults, and optional integrations.
- Likely codebase touchpoints:
  - `neuralforecast/models/`
  - `neuralforecast/models/__init__.py`
  - `neuralforecast/auto.py`
  - tests under `tests/test_models/` and related surface tests
