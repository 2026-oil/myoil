# Deep Interview Context Snapshot

- Task statement: Combine `data/BlackSwan_Core_Index_260319.csv` and `data/Data_Weekly_Tlab_260318.csv` into a single `df.csv`.
- Desired outcome: One merged dataset file, likely for downstream NeuralForecast/research use.
- Stated solution: Merge the two CSV sources and write `df.csv`.
- Probable intent hypothesis: Create a single modeling-ready weekly dataframe that includes both Tlab weekly features and BlackSwan core index features.
- Known facts/evidence:
  - `data/BlackSwan_Core_Index_260319.csv`: 584 data rows, 5 columns: `dt`, `BS_Core_Index_A`, `BS_Core_Index_B`, `BS_Core_Index_C`, `BS_Core_Index_Integrated`.
  - `data/Data_Weekly_Tlab_260318.csv`: 584 data rows, 86 columns; includes `dt` plus weekly market/macroeconomic columns.
  - The two files appear to represent the same weekly cadence but use different `dt` string formats (`YYYY-MM-DD` vs `YYYY.M.D`).
  - Repository memory notes mention prior use of `df.csv` in the broader research workspace.
- Constraints:
  - User explicitly invoked `$deep-interview`, so stay in requirements mode and do not implement yet.
  - Need a single-question Socratic loop.
  - Workspace currently has unrelated uncommitted changes; avoid touching them.
- Unknowns/open questions:
  - Desired merge semantics: join key/strategy (`dt` normalization + inner/left join?)
  - Output location/overwrite policy for `df.csv`
  - Desired final column order and date format
  - Whether any rows/columns should be excluded
- Decision-boundary unknowns:
  - Can OMX normalize `dt` and choose the natural all-column merge automatically?
  - Can OMX overwrite/create `df.csv` at repo root if absent/present?
- Likely codebase touchpoints:
  - `data/`
  - Possibly root-level `df.csv` or `data/df.csv`
  - Minimal one-off script or notebook cell if execution is later approved
